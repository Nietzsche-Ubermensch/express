{
  "name": "cardcrop-ai",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -v"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.0.0",
    "uuid": "^11.1.0"
  }
}
